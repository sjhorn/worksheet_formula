name: worksheet_formula
description: A standalone formula engine for spreadsheet-like calculations
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  a1: ^2.0.0
  petitparser: ^6.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
